# Gemfile

source "https://rubygems.org"
<% require 'rbconfig' # Have to do this because the version info is not maintained well 
%>
ruby "<%= RbConfig::CONFIG['prefix'].split('-').last %>"

# Development Utilities
gem 'awesome_print'
gem 'debug_me'
